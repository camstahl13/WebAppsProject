@model IEnumerable<ljcProject5.Models.LjcMinor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Minor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Minor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MinorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MinorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MinorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
